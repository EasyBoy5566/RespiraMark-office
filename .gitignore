# Python
__pycache__/
*.pyc
.venv/
venv/

# 本機設定（每台機器各自維護，範本見 config.json.example）
config.json

# 登入帳號、裝置權杖與 TLS 憑證（含私鑰與密碼/token 雜湊，絕不進 git；工具見 tools/）
accounts.json
devices.json
certs/

# Pi 系統狀態長期落地 CSV（每台伺服器各自累積，不進 git）
sys_logs/

# 警報事件歷史 CSV（每台伺服器各自累積，不進 git）
alarm_logs/

# 審計/運行日誌（每台伺服器各自累積，不進 git）
logs/

# 系統
.DS_Store
Thumbs.db


# claude
CLAUDE.md
