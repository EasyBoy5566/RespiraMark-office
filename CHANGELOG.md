# CHANGELOG

## 未發布 — 修正離線卡片與「無訊號」的語意

「無訊號」原本綁在 **Pi 與伺服器斷線**上（`.card.pi-offline .no-signal`），
所以關機中的樹莓派會在看板上留一張寫著「無訊號」的卡。兩件事都錯了：
關機的機器根本不該佔一張卡，而「無訊號」該講的是另一回事。

- **Pi 離線 → 整張卡片移除**（`destroyDev`），與管理員手動移除裝置走同一條路。
  snapshot 也跳過離線裝置，重新整理頁面不會又冒出來；裝置重新連上時卡片自動回來
- **「無訊號」改為「呼吸器已連線但波形沒進來」**：以最後一批波形的到達時間判斷
  （超過 3 秒；波形本來每 ~150ms 一批）。波形是持續流動的東西、沒有「停止」事件
  可聽，只能定時檢查
- **呼吸器未連線時改顯示 Pi 回報的狀態文字**（`status.msg`，例如「呼吸器未連線」）。
  否則這個狀態會是一張沒有任何說明的空白卡——比原本更難懂
- 看板因此開始註冊 `status`（Pi ↔ 呼吸器的串口狀態），原本刻意不註冊。
  還沒收到過 `status` 的裝置一律當作呼吸器已連線：Pi 只在狀態**變化**時才送，
  剛連上還沒變化過的不該被誤標成「未連線」
- 清掉隨之失效的死碼：`.card.pi-offline` 全部 CSS 規則、警報樣式裡的
  `:not(.pi-offline)` 守衛，以及只在離線時呼叫的 `clearLiveContent()`（41 行）
- ⚠️ **副作用**：使用中的 Pi 突然斷線（Wi-Fi 掉、拔電）時，那一床會直接從看板
  消失，而不是像原本變成警示卡。這與 W-304「離線卡片改高對比警示」的原意相反，
  但符合 2026-08-22 的指定行為與「不要裝置離線警報」的一貫決定。離線裝置仍完整
  列在 `/admin` 設備總表（含連線時長與最後狀態）
- 驗證：headless Chrome 實地跑四種狀態（關機不建卡、有波形無覆蓋文字、
  波形停 3 秒轉「無訊號」、呼吸器未連線顯示狀態文字），另驗斷線卡片消失、
  重連卡片回來

## 未發布 — 看板單位篩選（院方需求）

看板頂端新增「單位」勾選選單，只顯示選取單位的床。選單**動態產生**——看板上
有哪些單位就列哪些，不寫死清單（沿用 `ward.py` 的理由：寫死會讓漏列的單位整台
機器消失）。

- 選單分兩區：**加護病房**（MI、CCU、SI、RCC、PI、NI）與**病房**（其他），
  「未指定」獨立一項且預設勾選——剛配對還沒查到床號的機器不能因此從看板消失
- 新增 `ward.group_of()` 與協議欄位 `ward_group`（`icu`／`ward`／`""`），
  隨 `snapshot`／`device_meta` 送出。**分組規則放後端**：CLAUDE.md §5 明訂前端
  不得含業務規則，而「哪些代碼是加護」跟床號推導是同一類院內知識，放一起才不會
  日後兩邊各改一半。這是唯一寫死代碼的地方，而且只影響「排在哪一區」——沒列到的
  代碼（燒傷加護 `BC`、`IR`、`BR`）落在病房區，機器照常出現，最壞只是分錯區
- **存的是「不顯示」的集合，不是「要顯示」的集合**（選單本身仍是正向的，打勾＝顯示）：
  這樣新出現的單位一律預設顯示。若存正向清單，某天第一次有呼吸器推到沒出現過的
  單位時，它會因為不在清單裡而悄悄不見——看板漏一床的代價遠大於多顯示一床
- **被篩掉的床不鳴響警報音**：聽得到卻在畫面上找不到來源的警報聲比不響更糟
  （真正的臨床警報在呼吸器本體上）
- **篩選中一定看得出來**：按鈕轉琥珀色，另外在頂端顯示常駐提示
  「單位篩選中：CCU（已隱藏 5 台）」——狀態列收起也看得到，免得有人忘了自己設過
  篩選，把「被隱藏的床」誤判成「沒接上的床」
- 全部被篩掉時空畫面改說「目前篩選條件下沒有裝置（共 N 台被隱藏）」，不再顯示
  會誤導的「等待裝置連線中」
- 其他細節：面板打開時把自動收起的狀態列鎖住；末列空位改依**可見**卡片數補齊；
  放大中的卡片被篩掉會先收回；監視器模式下全部隱藏時收掉格線 padding（否則會
  露出一條近白的邊）
- 驗證：新增 6 項 `test_ward` 分組測試（全套 186 項通過）；前端另用 headless
  Chrome（DevTools Protocol）實地載入看板操作選單，確認分組、篩選、常駐提示、
  空畫面文案、空位補齊與全選還原都正確，主控台無錯誤
- 修掉一個自己寫進去的地雷：「未指定」的內部代號原本誤含一個 NUL 位元組
  （原始碼裡看不見的控制字元），改為 `__unassigned__`

## 未發布 — 備份改為週備份、警報與系統健康納入備份

備份原則定案（對應資訊室表單二「備份」欄的「週備份留 3 週」）：**每週備份一次、
保留最近 3 份、四週循環**。原本刻意排除在備份外的警報 episode 與 Pi 系統健康
兩個 SQLite，改為一併納入。

- 新增 `tools/backup_db.py`：走 SQLite 官方 backup API 取一致快照，**來源可以正在
  被伺服器寫入、不需停機**，備份後自動跑 `PRAGMA quick_check`。
  🚨 **SQLite 絕不能用複製檔案的方式備份**——DB 跑 WAL 模式，最近的異動還在 `-wal`
  裡，運行中 `Copy-Item` 會拿到撕裂的檔案：平常看不出來，真的要還原時才發現壞的
- `tools/backup.ps1` 改寫：
  - 納入 `logs\audit_logs\`、`alarm_logs\`、`sys_logs\` 的 SQLite（全走 backup_db.py）
  - **修掉一個實質缺陷**：原本只備份 `logs\audit.log` 一個檔，輪替後的
    `audit.log.YYYY-MM-DD` 全部沒進備份——還原後只剩當天那份，交代不了防護基準
    第 16 點的 6 個月保存。改成 `audit.log*` 一次帶走全部輪替檔
  - 保留策略由「刪掉 N 天前的」改為「**保留最近 N 份**」（預設 3）：某週因為機器
    關機而漏跑時，依日期刪除有機會把僅存的備份全部清光
  - 新增 `-Register`：註冊每週日 03:00 自動執行的工作排程（`-StartWhenAvailable`，
    當時關機則開機後補跑）
  - 補上 `-WhatIf` 預覽（README 一直宣稱每支維運腳本都支援，這支其實沒有）
  - 檔案補上 UTF-8 BOM——Windows PowerShell 5.1 讀 .ps1 預設用 ANSI，沒有 BOM 的
    中文註解會讓整支腳本解析失敗（其他三支維運腳本本來就有 BOM）
- 驗證：實跑一次備份後解開 zip 比對，`system_sample` 2549 筆／`alarm_episode`
  687 筆與正本完全一致、`PRAGMA integrity_check` 皆為 ok、zip 內無 `-wal`／`-shm`
  殘留；輪替與 `-Register` 亦實測正確
- ⚠️ **注意保留期的落差**：alarm/sys 本機只留 7 天，週備份等於零餘裕——排程一旦
  延誤就會在歷史上留下空隙。若要保險，可把 `alarm_retention_days`／
  `sys_retention_days` 調到 10，讓相鄰兩次快照重疊 3 天（磁碟成本約 +40%，
  仍在數百 MB 等級）。目前**維持 7 天未改**

## 未發布 — 床號自動帶入（院方設備系統 maya）

床號一直是空的：`bed` 欄位、`ward` 推導、看板依床號排序都早就做好了，缺的是
**上游來源**。醫療人員接呼吸器時會在 maya 上更新該台的使用位置，這次把那份資料
接進來，床號從此不需要人工維護。

- 新增 `transport/maya_client.py`：查 `getDeviceList`、解析成「財編 → 床號、記錄時間」。
  全形／半形冒號混用都認（院方實際回傳就是混用）、帶床位序號的 `3520-1` 不會被截斷、
  記錄時間缺漏時退回同筆的 `RECORD_DATE`、同一財編多筆取最新、單筆壞掉只跳過該筆
- 新增 `domain/bed_sync.py`：排程與規則。呼吸器連上記為 A → 每 30 秒查一次 →
  記錄時間比 A 新就採用並停查；呼吸器斷線取消、Pi 離線暫停、逾一小時放棄
- 🚨 **只取床號；姓名與病歷號在解析當下就丟棄**——`Position` 裡根本沒有這兩個欄位，
  下游想留也留不到。CLAUDE.md §2.2 紅線：床號不是個資，姓名與病歷號是。因此這個
  功能**不會**改變本系統的個資範圍，防護等級評估不用因此重評
- **時間容差方向刻意偏向「接受」**（記錄時間 ≥ A − 120 秒即算新）：時鐘只靠 NTP 對齊，
  醫護也常在接機前一兩分鐘就先更新 maya。若反過來要求晚於 A ＋ 容差，那些正確的
  資料會被永遠拒絕、白白輪詢滿一小時
- **Pi 離線是暫停不是取消**：使用者原本的規則是「斷線就停」，但照字面實作的話
  Wi-Fi 抖一下就永久放棄查詢、床號會莫名其妙一直空著。呼吸器斷線才是真的取消
  （那代表沒有新病人要記錄）
- **一次請求涵蓋全部待查裝置**：`getDeviceList` 回的是全院清單（數百筆），每輪只發
  一個請求再逐台比對。沒有查得動的裝置（全部完成／離線／未登記財編）時完全不發請求——
  對院方正式系統的請求量能少一次就少一次
- `maya_enabled` **預設 false**，新增 `maya_url`／`maya_poll_interval`／`maya_timeout`／
  `maya_max_duration`／`maya_time_tolerance` 設定
- Hub 新增 `attach_bed_sync()` 與 `is_online()`，並在 hello／status／remove_device
  三處掛勾。**領域層不 import 傳輸層**（CLAUDE.md §3.1）：BedSync 的 fetch 與寫回
  都是注入的 callback，由 `main.py` 這個 composition root 接起來
- 新增 `tools/fake_maya.py` 本地假伺服器（`--stale-for` 可演練「等 maya 更新」的過程）。
  🚨 **絕不對 maya-ap 發任何請求**，自動化測試一律只打 127.0.0.1
- 新增 `tests/test_maya_client.py`（18 項，含「姓名與病歷號不得出現在解析結果」的紅線測試）
  與 `tests/test_bed_sync.py`（19 項狀態機）；全套 180 項與 `smoke_test.py` 通過

## 未發布 — 單位（由床號推導）

看板要能「只顯示某幾個單位的呼吸器」，管理頁要能看出「這台在哪個單位」。

- 新增 `domain/ward.py`，處理院內兩種床號格式：
  - **加護／特殊單位**：代碼寫在床號開頭，取連續英文字母
    （`MI01`→`MI`、`CCU03`→`CCU`、`RCC21`→`RCC`、`IR01`→`IR`、`BR21`→`BR`、
    `HP13-1`→`HP`）
  - **一般病房**：`{樓層2碼}{房間2碼}-{床位}`，三個系列各有換算——
    N08–N17 是 +20（`3520-1`→`N15`）、R07/R09–R14 同號（`0711-1`→`R07`）、
    M/MP 是 +80（`8811-1`→`M08`、`8611`→`MP06`，身心科一房一床無床位序號）
  - 樓層碼不在表上仍退回**一般病房**而非未指定：呼吸器有時會在病房使用，
    歸不了類的話那些機器會因為篩不到而整台從看板消失
- **刻意不寫死單位清單**：院方 `get_inPatient_wardList()` 的 33 個單位代碼表
  **不完整**——燒傷加護（`BC`）是真實單位卻不在表上，而實際設備資料裡有
  BC02。寫死成 `^(MI|CCU|SI|…)` 會讓它比對不到、該台機器整個從看板消失
- **G 院區（NSH9／NSH10／NSR11）不支援**：判斷 G 院區要看 `Zone`，而
  `getDeviceList` 沒這個欄位；且 `A01-1` 會被判成單位 `A`、`NSH10` 的
  `1001-2` 與 `R10` 的 `1011-1` 撞樓層碼。實測呼吸器清冊裡 G 院區掛零，暫不處理
- **單位不另存欄位**，由床號即時推導：多存一份只會多一份會過期的資料。
  伺服器算好後隨 `snapshot`／`device_meta` 送出，前端只負責顯示與篩選
  （CLAUDE.md §5：前端不得包含業務規則）
- 管理頁表格新增「單位」欄（放在床號前，層級上單位 > 床號），搜尋一併涵蓋
- `set_meta` 的回應改為明確列出 `device`/`bed`/`ward`/`asset`——原本順手回整包
  meta 會把管理員備註 `note` 一起送出去，那不在 PROTOCOL.md 的契約裡
- 新增 `tests/test_ward.py`（8 項，案例全取自院方設備系統的實際床號）；
  全套 139 項與 `smoke_test.py` 通過

## 未發布 — 管理頁設備總表改為對齊表格

原本每台裝置是一張切成三欄的卡片（識別｜四指標｜資訊+操作），問題有三個：
三欄之間用實線切開，同一台機器的資訊看起來像三張卡拼的；每張卡是獨立的 grid，
**卡與卡之間欄位不對齊**，等於放棄了列表最大的優勢；14 個資訊欄位加 5 顆按鈕
全部同一個視覺重量，右欄還會因為塞不下而出現橫向捲軸。

- **改成一台一列的對齊表格**（`<table>` + 每台一個 `<tbody>`）：CPU／溫度這些
  數字排在同一條垂直線上，異常值自己會跳出來。1280×720 一屏約 12–14 台
- **主列只留掃描要看的**：連線燈／機台編號／床號／財編／呼吸器／連線時長／
  CPU／記憶體／溫度／磁碟／「詳細」展開鈕
- **次要資訊與全部操作收進展開列**：版本、剩餘空間、開機時長、降頻旗標
  （原本就已重複顯示在展開區）＋ 財編、CSV、警報紀錄、移除四顆按鈕。
  危險操作藏在展開後，比跟「下載 CSV」並排安全
- **離線整列轉灰**取代原本單獨一顆連線標籤——比一顆燈更難忽略
- 移除已無人使用的 `.status-group`／`.status-tag`／`.link-status`
  與整組卡片版面 CSS；窄螢幕改為表格橫捲，不再直向堆疊（那會讓每台又縮回
  一團自成一格的資訊，正是這次要解決的問題）
- 順手修掉帳號清單那段仍在講 `auth_backend` 與「ldap 模式」的說明文字——
  該設定與實作已於 2026-08-11 移除

## 未發布 — 裝置連線時刻與軟體版本

管理頁原本只看得到「線上／離線」與四個系統指標，答不出兩個機隊管理的基本問題：
**這台連上多久了**、**它在跑哪一版程式**。連線事件雖然有寫進 `audit.log`，但只是
純文字，畫面上查不到。

- `hello` 新增 **`app_version`**（選填）：Pi 端帶上自己的 `respiramark.__version__`。
  舊版 Pi 不送此欄 → 伺服器一律視為空字串，管理頁顯示 `—`。**純新增欄位，協議版號不變**
- `DeviceState` 新增 **`connected_at`**（伺服器產生的本次連線起點）與 `app_version`，
  `snapshot` 與上線的 `link` 都帶出去
- **watchdog 誤判離線後回復上線時不重設 `connected_at`**——TCP 連線從未中斷，
  重設會讓連線時長被一次網路抖動歸零而失真；只有真正的新 `hello` 才重設
- 管理頁卡片新增「已連線」與「版本」兩個資訊 chip，沿用既有 `info-chip` 樣式
  （不動 CSS）。連線時長是相對時間，由前端每 30 秒自行走動，只改該顆 chip 的
  文字而不重跑 `renderCard`（後者會連展開中的趨勢圖一起重畫）
- 時間格式化留在前端（CLAUDE.md §5）：伺服器只廣播 `connected_at` 這個原始時刻。
  ⚠️ 前端以瀏覽器本機時間相減，因此**依賴伺服器與看板電腦時鐘同步**（NTP，W-206）
- `tools/fake_pi.py` 送 `1.0.0-fake`，管理頁一眼分得出哪幾台是模擬器
- `tests/test_hub.py` 新增 6 項鎖住上述行為；全套 131 項與 `smoke_test.py` 通過

## 未發布 — 資訊室會議定案的合規調整（2026-08-11）

依 2026-08-10 與中山附醫資訊室第一次會議決議，以及院方「資通系統防護需求等級
評估表及防護基準表」（227000-000-F-032）的中級適用項目調整：

- **移除 LDAP/AD 帳密驗證**：會議定案登入改走 **HIS 帳號密碼**（院方 LDAP 僅供
  院內個人服務，不適用本系統）。刪除 `monitor/web/ldap_auth.py`、
  `tests/test_ldap_auth.py`、`ldap3` 相依，以及 `auth_backend`／`ldap_*` 設定欄位。
  **保留驗證器的依賴注入抽象**——HIS 介接規格到手後，只需新增一個實作
  `authenticate()`／`has_users()`／`list_users()` 的類別並在 `main.py` 的
  `build_authenticator()` 選用，登入頁、session、middleware 都不用動
- **登入鎖定時間 10 分鐘 → 15 分鐘**（防護基準 42 要求失敗 5 次後至少鎖 15 分鐘）
- **日誌時戳改 ISO 8601 含時區位移**（防護基準 24 要求可對應 UTC）：原本
  `server.log` 只有 `%H:%M:%S`、連日期都沒有，跨日就分不出是哪天；`audit.log`
  雖有日期但無時區位移，跨系統比對無從還原成 UTC
- **日誌改每日輪替、保留 190 天**（防護基準 16 要求正式系統保留至少 6 個月）：
  原本是 10MB×5 的大小輪替，只保證留最近 50MB，日誌一忙就會把幾個月前的紀錄
  擠掉。新增 `log_retention_days` 設定，低於 180 會被拉回下限並於啟動時警告
- **未預期例外收斂**（防護基準 55）：新增 `error_middleware`，對外只回簡短訊息
  與一組隨機代碼，完整 traceback 只進伺服器日誌；代碼供使用者回報後定位。
  它刻意排在 `security_headers_middleware` 內層，讓 500 也帶得到安全標頭
  （原本 500 是唯一漏掉安全標頭的回應）
- **`config.json.example` 的 `web_port` 改 443**：醫院 VM 部署走 HTTPS 443 ＋
  ingest 8765（已填入資訊室申請表的服務埠欄）。程式**預設值仍是 8080**——443 是
  HTTPS 慣用埠，只有同時設定 `tls_cert`/`tls_key` 才該使用，預設不開 TLS 的開發
  環境若也用 443 會變成在 443 上跑明文
- **裝置配對支援 TLS**：Pi 端配對客戶端原本只講明文 HTTP，伺服器一開 TLS 就配不了
  對（只能退回 `tools/make_device.py` 手動核發）。現在給了 CA 就走 HTTPS 並驗證伺服器
  憑證，憑證驗不過一律拒絕、不會退回明文重試；憑證錯誤與連不上分開提示，現場才查得
  下去。`tools/fake_pi.py --pair` 同步支援 `--tls-ca`，可無真機演練 TLS 配對
- 新增 `tests/test_error_middleware.py`（6 項）與 `tests/test_log_setup.py`（6 項）
  鎖住上述合規行為；全套 125 項與 `smoke_test.py` 通過

## 未發布 — 警報依實機優先值分級

- Office 警報改以 Pi 實際回報的 MEDIBUS `prio` 判級：P25～31 為 level 1、P11～24 為
  level 2、P1～10 為 level 3；同一警報碼在不同裝置上的優先值不同時也能正確呈現
- `(cp, code)` 對照表保留完整名稱與備援 level；只有舊紀錄缺少 `prio` 或收到異常值時
  才使用固定對照，並依實機紀錄同步修正 5 筆備援值，避免既有歷史資料失去或誤用分級

## 未發布 — 床號與呼吸器財編

- 看板小卡標題由機台編號改為**床號**；還沒指定床號的退回顯示機台編號並以較淡的
  樣式標示，看板改依床號排序（未指定的排在最後）
- 每台裝置可綁定**呼吸器財編**，對應實體機器供盤點與報修；刻意只顯示在管理頁，
  不佔用看板小卡的標題列
- **床號不開放人工設定**：規劃由財編向院內系統查詢後自動帶入，管理 API 仍接受
  `bed` 供屆時寫入，但管理頁只提供財編輸入——多一個人工來源只會多一份會過期的資料
- 兩者都存在伺服器的裝置清冊（`devices.json`），Pi 端不參與；管理頁新增財編編輯與
  搜尋框（床號／機台編號／財編任一符合）
- 管理頁四個系統指標（CPU／記憶體／溫度／磁碟）縮小字級與間距、中欄壓窄，
  多出來的寬度讓給左欄
- 管理頁卡片標題一律是**機台編號**（看板才以床號為標題），床號與財編各自是一個
  帶標籤的欄位，未綁定顯示 `--`；排序也改依機台編號，與標題一致
- 新增 `PUT /api/admin/devices/{device}/meta` 與 `device_meta` 廣播：管理員改完，
  所有看板即時換標題與重新排序，不需重新整理
- **只有已登記在 `devices.json` 的裝置能設定**：清冊檔不存在時回 409 而不建檔——
  否則會把伺服器從共用權杖模式切換成逐台驗證，讓既有裝置重連被拒
- `devices.json` 的讀改寫集中到新的 `domain/device_directory.py`（配對核發與床號
  編輯共用同一套原子寫入）；換發權杖時保留床號、財編與管理員備註

## 未發布 — 裝置配對

- 新增裝置配對流程，取代「在伺服器產 token 再手動貼到 Pi」：Pi 送出申請 → 管理員在
  `/admin` 核對 6 位確認碼後核可 → Pi 自動領取 token 寫入自己的 `telemetry.json`。
  管理員全程不會看到 token（`tools/make_device.py` 保留為 fallback）
- 走 HTTP `:8080` 的 `/api/pair/*`（Pi，免登入）與 `/api/admin/pair/*`（admin）；
  TCP 8765 的 ingest 協議完全不動
- token 在 poll 回應中只出現一次，回傳同時銷毀整筆配對；待核可資料只存記憶體，
  伺服器重啟即失效。TTL 10 分鐘、同時待核可上限 5 筆、同一來源 IP 只留最新一筆
- 重複配對視為換發，核可頁顯示警告並要求二次確認；審計記錄 `pair_requested`／
  `pair_approved`／`pair_claimed`／`pair_denied`，一律不含 token
- `devices.json` 讀寫分離：驗證（讀）仍在 `transport/device_auth.py`，寫入改由新的
  `domain/pairing.py` 以 tmp + fsync + os.replace 原子寫入
- 新增設定 `pair_enabled`／`pair_ttl`／`pair_max_pending`；TLS 啟用時 Pi 端配對客戶端
  連不上（只支援明文 HTTP），伺服器啟動時記警告並導向手動核發
- `tools/fake_pi.py --pair` 可在沒有真機時演練管理頁的核可畫面

## 未發布 — 單床詳細監測工作台

- 全部執行期日誌集中到 `logs/`；警報 DB 位於 `logs/alarm_logs/alarm_history.sqlite3`，
  系統狀態 DB 位於 `logs/sys_logs/sys_history.sqlite3`
- 系統狀態由每台機器一個 CSV 改為單一 SQLite，維持 60 秒落地節流並自動保留 7 天；
  管理員下載時才即時產生 CSV，伺服器端不留匯出檔
- 移除既有舊版 Alarm CSV 與 Sys CSV；alarm/sys 七天歷史 SQLite 皆不納入備份
- 點擊小卡後改為單一視窗工作台：上半部以 2:1 顯示三條即時波形＋設定值／測量值，
  下半部以 1:1:1 顯示 LOOP／最近警報紀錄／預測模組預留區
- LOOP 使用單一大圖，combo box 可切換 P-V、F-V、P-F；保留最近三個完整呼吸，
  每圈嚴格以吸氣 trigger 到下一個 trigger 繪製，最新曲線標示 trigger 起點，前兩次以淡線疊加，
  選擇會保存在瀏覽器
- 警報紀錄改用單一 SQLite 儲存完整 episode；各機台可獨立查詢，詳細畫面顯示最新 50 段的
  開始、結束、持續時間與狀態，管理員 CSV 改由資料庫即時匯出
- 已結束 episode 自動保留 7 天，作用中警報不會逾期刪除；裝置離線或伺服器重啟時標記為
  「結束不明」，避免誤記為已解除。只保存機台與警報內容，不含病人代碼、波形或量測值
- 依目前儲存資源決策，警報 SQLite 不納入 `backup.ps1`
- 大卡警報紀錄改用主題一致的細版捲軸，放大靜音按鈕；三種主題的大、小卡靜音鈕與
  大卡關閉 X 均改為透明無框樣式
- 預留「拔管成功率」與「呼吸不同步預測」兩個模組插槽；模型未接入前不顯示假數值
- 桌面版於 1920×1080、1366×768 維持單視窗，小型畫面改為可捲動直向配置
- Level 1／2／3 警報全面改為共用 `alarm_synth.js` 的 Web Audio 振盪器合成音，網頁不再載入
  MP3；合成器移至靜態資源根目錄並移除舊 `alarm-sounds` 資料夾
- Level 1 兩組五音的第 5 音由約 0.15 秒延長為 0.20 秒；每段合成音完整播放後才靜音
  2.5 秒，警報仍存在才重播，多警報同時存在時只播最嚴重者
- 警報音試聽頁改為純合成音調音工具，可分別試聽三個等級且不需音檔
- `fake_pi --alarms` 改為無作用中警報時每次檢查只有 3% 機率觸發，避免一次模擬 20 台時
  持續充滿警報聲；可用 `--alarm-probability` 調整，`--alarm-immediate` 仍供測試強制首發
- `fake_pi` 在本機 `devices.json` 模式會自動安全登記測試裝置；同次執行的模擬裝置
  共用一組臨時 token，只計算一次 PBKDF2 雜湊以加快多台啟動。明文不落地，也不允許
  覆寫既有正式裝置，修正預設模擬器遭權杖驗證拒絕的問題

## v1.2.0 — 警報音改為呼吸器警報專用、移除離線提示音、ldaps 憑證驗證（2026-07-17）

你確認「裝置離線警報（提示音）」用不到，聲音只在**呼吸器本身發出警報**時才需要，
且靜音操作要放在卡片上。另確認院內憑證統一由資訊室簽發，補上 ldaps 的憑證驗證。

- **ldaps 伺服器憑證驗證**：`config.json` 新增 `ldap_ca`（院內 CA 根憑證檔）——
  設定後 LDAP 連線以 `CERT_REQUIRED` 驗證 AD 伺服器憑證，防止院內網有人冒充 AD
  收集帳密；未設定維持只加密不驗證並於啟動時警告（正式環境必填）。CA 檔路徑錯誤
  一律 fail closed（驗證失敗），不會讓例外炸到登入路由。單元測試新增 3 項（共 71 項）。
  README 補「院內 CA 簽發憑證」部署說明（自建 CA 降為開發測試用）

- **移除裝置離線提示音**（W-304 的提示音部分）：header 選單的「離線提示音」開關、
  localStorage 設定（`rm-offline-sound`）與離線嗶聲整組拿掉；離線仍保留紅框高對比
  視覺提醒不變
- **新增呼吸器警報音**：裝置回報 level 1/2 警報（依 `alarm_levels.js` 分級）時鳴響
  ——level 1（危及生命）三連短高音、每 2.5 秒；level 2 兩連低音、每 5 秒；多台同時
  警報時取最嚴重等級。level 3 與 Pi 離線（資料不可信）不鳴響。純 Web Audio 合成，
  零相依；受瀏覽器自動播放政策限制，使用者需與頁面互動過一次才會出聲（解鎖前
  只有視覺警示）
- **卡片警報列新增靜音鈕**：只靜音該台裝置，不影響其他床位；該台警報**全數解除後
  自動復歸**（下次警報會再響），避免忘記取消靜音而漏掉新警報
- `smoke_test.py` 檢查項同步更新（確認離線提示音開關已移除、警報音程式已就位）

## v1.1.0 — LDAP/AD 帳號整合上線、移除本機改密碼、Header 選單改版（2026-07-07）

你確認要用院內 LDAP/AD 取代本機密碼自助管理（W-307 提前實作，不再是選做），
並要求把 header 一整排按鈕收整成下拉選單。

- **LDAP/AD 帳號驗證**（W-307）：新增 `monitor/web/ldap_auth.py`
  （`LdapAuthenticator`），`monitor/web/auth.py` 的 `AuthManager` 改為依賴注入
  （不再寫死 `LocalAuthenticator`），`config.json` 新增 `auth_backend`
  （`local`/`ldap`）等欄位。**預設仍是 `local`**，等資訊室提供實際 LDAP 連線
  資訊後只需改設定檔即可切換，不用改程式碼。新增相依套件 `ldap3==2.9.1`
  （僅 `auth_backend=ldap` 時需要安裝，已於本次獲你核准）
- **移除密碼自助管理功能**（W-303 整組移除）：自助改密碼（`POST /api/password`）
  與管理員重設密碼（`POST /api/admin/reset-password/{username}`）連同前端
  「改密碼」按鈕/彈窗全部拿掉——密碼管理方式改為單純依 `auth_backend`：
  `local` 模式由管理員在伺服器執行 `tools/make_user.py`，`ldap` 模式密碼完全
  交給院內 HIS/AD
- **Header 改成帳號下拉選單**：深淺色切換、離線提示音、設備管理、登出等
  「動作類」項目收整到點帳號名稱展開的下拉選單，取代一整排按鈕
- 單元測試新增 `tests/test_ldap_auth.py`（8 項，用 `ldap3` 內建 `MOCK_SYNC`
  策略模擬 LDAP 目錄，不需要真的院內環境即可驗證 bind 成功/失敗/未知帳號/
  不在白名單/空密碼等情況），總數達 68 項；`tests/smoke_test.py` 移除已不存在
  端點的測試段，其餘全數通過

## v1.0.0-rc1 — Phase 3 完成：功能補強（2026-07-07）

對應 IMPROVEMENT_PLAN.md Phase 3「功能補強」完成定義（G-01/G-02/G-04/G-05
關閉）。**注意**：本版包含的密碼自助管理功能（W-303）已於後續 v1.1.0 移除，
詳見上方說明。

- 免責聲明「本畫面僅供觀察參考，不可作為臨床警報依據」三頁常駐顯示（W-301）
- 警報事件歷史記錄：`domain/alarm_log.py` 記錄出現/解除時間、代碼、優先級到
  CSV（只記機台編號，不記病人代碼），管理頁可下載（W-302）
- 密碼自助管理：登入者自助改密碼、admin 可重設任一帳號密碼（W-303，
  **已於 v1.1.0 移除**）
- 裝置離線醒目提醒改高對比樣式 + 可選提示音（W-304）
- `/healthz` 健康檢查、版本號顯示（W-306）
- 正式伺服器維運腳本：`tools/setup_service.ps1`（開機自動啟動+自動重啟）、
  `tools/lock_permissions.ps1`（鎖敏感檔案權限）、`tools/backup.ps1`（自動
  備份，不含 CA 私鑰）、時間同步（NTP）文件

單元測試累計達 60 項，冒煙測試同步擴充涵蓋警報歷史、密碼自助管理等新端點。
W-305（歷史波形回放）依計畫維持選做、不做。

## v0.11.0 — Phase 2 完成：可無人值守運行（2026-07-07）

伺服器可自動啟動、自動復原、有完整日誌、有備份——對應 IMPROVEMENT_PLAN.md
Phase 2「伺服器可無人值守運行」的完成定義。

- 一般運行 log 落地 `logs/server.log`（W-201），與既有審計日誌並存
- `GET /healthz` 免登入健康檢查端點 + 集中版本號定義（W-203）
- 免責聲明「本畫面僅供觀察參考，不可作為臨床警報依據」三頁常駐顯示（W-301）
- 裝置離線提醒改高對比樣式（不再靠調暗，改紅框+紅底更醒目）+ 可選提示音（W-304）
- 給正式伺服器用的維運腳本：`tools/setup_service.ps1`（開機自動啟動+自動重啟）、
  `tools/lock_permissions.ps1`（鎖敏感檔案權限）、`tools/backup.ps1`（自動備份，
  不含 CA 私鑰）（W-202/204/205）
- README 補時間同步（NTP）與 ca.key 離線保存說明（W-206）
- `tools/soak_monitor.ps1` 補明確判讀門檻（W-207）

W-202/204 的腳本已寫好並用 `-WhatIf`/`-DryRun` 驗證過語法與行為，實際在正式
伺服器上套用（真的建立排程工作、真的鎖權限）留待 Phase 5 醫院移轉時執行。

## v0.10.0 — Phase 1 完成：F-01～F-15 資安強化（2026-07-07）

- **每台 Pi 獨立存取權杖**（`devices.json` + `tools/make_device.py`）：外洩只需
  停用/換發該台，不再牽連全部裝置（F-01，最主要項目）；未設定時退回舊版單一
  `ingest_token`，向後相容
- ingest 連線防護：同時連線數上限、hello/閒置逾時三重防護；訊息格式驗證
  （wave/params 陣列與鍵數上限）；WebSocket 觀看端數上限（F-02/F-03/F-15）
- token 比對改常數時間比較，防 timing attack（F-07）
- HTTP 安全標頭 middleware（CSP `default-src 'self'`、X-Frame-Options 等）；
  `/ws` 加 Origin 同源檢查（F-04/F-05/F-11）
- session 絕對逾時 + 定期掃除 + 總量上限；登入鎖定改雙鍵（IP+帳號 / IP 總量），
  避免共用電腦時一人打錯密碼連累其他人（F-08/F-09）
- PBKDF2 迭代數 20 萬→60 萬次，舊帳號/裝置零風險相容升級（F-10）
- 新增審計日誌 `logs/audit.log`，內建病人代碼/密碼/token 防呆（F-06）
- `requirements.txt` 鎖定確切版本；過程中意外發現並修正 cp950 語系 Windows
  下 pip 解析 requirements.txt 失敗的問題（F-12）

單元測試由 17 項擴充到 50 項，冒煙測試同步大幅擴充。F-13/F-16 需正式伺服器
環境才能執行，留待 Phase 2/5；F-14（CSRF）評估後決定暫不處理。

## v0.9.0 — 醫院前基線（2026-07-07）

進醫院資訊室談伺服器架設之前的基線版本。已具備：

- 三層架構（transport/domain/web），composition root 在 `main.py`
- TLS 加密（自建 CA + 憑證，`tools/make_certs.py`），憑證缺失時拒絕啟動
- 瀏覽器登入（PBKDF2-SHA256 密碼雜湊、伺服器端 session、viewer/admin 角色、登入失敗 IP 鎖定）
- 管理頁 `/admin`：設備健康總表、CPU/記憶體/溫度/磁碟/降頻趨勢、移除離線裝置、sys CSV 下載、帳號唯讀清單
- 多床即時儀表板：三波形（抖動緩衝平滑播放）、通氣模式、設定值、量測值、**三級警報分色**（依 MEDIBUS.X IFU 分級對照表）
- Pi 系統健康遙測（CPU/記憶體/溫度/磁碟/降頻/開機時長），CSV 落地並節流
- 深/淺色主題、自動重連
- 開發配套：`tools/fake_pi.py` 模擬器、hub 單元測試、涵蓋 TLS/登入/角色/管理功能的端對端冒煙測試
- `IMPROVEMENT_PLAN.md`：醫院上線前完整資安與功能評估、分階段工作清單（Phase 0～5）

已知風險與後續計畫詳見 `IMPROVEMENT_PLAN.md`。下一步：Phase 1 資安強化（每台 Pi 獨立 token、連線防護、訊息驗證、安全標頭、session 強化、審計日誌等）。
