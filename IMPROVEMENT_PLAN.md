# RespiraMark Office — 醫院部署前 資安與功能評估暨改善計畫

- 文件版本：v1.4（2026-07-07；Phase 0～3 全數完成，依序打 tag `v0.10.0`／`v0.11.0`／`v1.0.0-rc1`；F-01～F-15、G-01～G-09 已關閉，詳見第二/三節「狀態」欄。W-307（LDAP/AD）已提前完成、W-303（密碼自助管理）已移除，見該二列狀態欄與下方「2026-07-07 後續變更」說明。剩 Phase 4（部署前整體驗證）、Phase 5（醫院移轉，需資訊室配合）、W-305（選做，不做））
- 評估範圍：respiramark-office 全部程式碼（後端 `main.py` + `monitor/`、前端 `monitor/web/static/`、工具 `tools/`、測試 `tests/`）、與 Pi 端遙測客戶端（`respiramark-pi/respiramark/web/telemetry_client.py`）的介接、伺服器部署方式
- 文件用途：作為醫院上線前的**工作清單與驗收依據**。每個工作項目（W-xxx）都有對應測試項目（T-xxx），照 Phase 順序逐一完成、逐一勾選，全部完成即可移轉到醫院伺服器
- 遵循原則：所有改善方案均符合本專案 CLAUDE.md 鐵則（相依只有 aiohttp、前端零框架、資料不離開院內網、病人代碼不進 log）

---

## 一、系統現況總覽

```
Pi #1..#16 ──TCP :8765（JSON Lines，TLS 可選，共用 token）──▶ 彙整伺服器（單一 Python 程序，aiohttp）
                                                                    │
瀏覽器（護理站/管理員）◀──HTTPS :8080 + WebSocket /ws（登入 session）─┘
```

| 面向 | 現況（Phase 1 完成後，2026-07-07） |
|---|---|
| 程式架構 | 三層（transport / domain / web），composition root 在 main.py；新增中性共用模組 `monitor/crypto.py`、`monitor/audit.py`（config.py 同層級，供任一層 import） |
| 傳輸加密 | 自建 CA + TLS 1.2+（兩個 port 同時加密），CA 釘選，憑證缺失時拒絕啟動（不會靜默退回明文） |
| Pi 端驗證 | **每台裝置獨立 token**（`devices.json` + `tools/make_device.py`，PBKDF2 雜湊）；未設定 `devices.json` 時退回單一共用 `ingest_token`（向後相容）。ingest 另有連線數上限、hello/閒置逾時、訊息格式驗證 |
| 瀏覽器驗證 | 帳密登入（PBKDF2-SHA256 60 萬次雜湊）、伺服器端 session（閒置逾時 + 絕對逾時 + 總量上限定期掃除）、HttpOnly/SameSite=Lax/Secure cookie、viewer/admin 角色、登入失敗雙鍵鎖定（IP+帳號 與 IP 總量兩層） |
| HTTP 安全 | 安全標頭 middleware（CSP `default-src 'self'`、X-Frame-Options、X-Content-Type-Options、Referrer-Policy、TLS 時 HSTS）；`/ws` Origin 同源檢查 |
| 審計 | `logs/audit.log`（10MB×5 輪替）記錄登入/登出、管理操作、裝置上下線與被拒事件，內建病人代碼/密碼/token 防呆 |
| 資料落地 | 波形/參數不落地；Pi 系統健康與警報皆以 SQLite 保存最近 7 天，CSV 僅在下載時即時產生。兩者皆不含病人代碼 |
| 前端 | 原生 HTML/CSS/JS，零外部資源（無 CDN），使用者資料以 textContent 寫入（防 XSS 的正確作法） |
| 測試 | 50 項單元測試（hub / auth / crypto / device_auth / ingest 裝置模式）+ 端對端冒煙測試（涵蓋 TLS、登入、每台獨立 token、安全標頭、Origin 檢查、連線防護、審計日誌、角色權限、管理功能） |
| 部署方式 | 手動雙擊 start_server.bat；**尚無**服務化、開機自啟（Phase 2 W-202，待辦） |

### 已具備、應保留的資安設計（評估結論：基礎良好）

1. TLS 憑證缺失即拒絕啟動——「寧可不啟動，不靜默明文」是正確決策
2. 密碼只存 PBKDF2 雜湊、格式自帶迭代數（未來提高迭代不會弄壞舊帳號）、比對用 `hmac.compare_digest`、帳號不存在時跑假雜湊抹平時間差
3. session token 用 `secrets.token_urlsafe(32)`（256-bit），cookie 本身不含資訊
4. 登入 middleware 是「預設拒絕」：除 `/login` 與 `/static/` 外全部要 session
5. 帳號清單 API 絕不回傳密碼雜湊；登入失敗 log 不記帳號名
6. 病人代碼不寫 log、不寫 CSV；accounts.json / certs/ / config.json 都已 gitignore 且確認未進 git
7. CSV 下載端點的裝置名有 `_safe_name()` 過濾（無路徑穿越）；靜態檔服務未開目錄列表
8. ingest 有單行長度上限、裝置數上限、壞 JSON 容錯不崩潰；廣播佇列滿丟最舊絕不回壓
9. 冒煙測試把「未登入被拒、錯密碼被拒、錯 token 被斷線、viewer 進不了管理頁」都當必過項
10.（Phase 1 新增）每台 Pi 獨立 token，外洩只需停用/換發該台，不影響其他裝置
11.（Phase 1 新增）ingest 連線數/hello/閒置逾時三重防護，訊息格式驗證，觀看端數上限
12.（Phase 1 新增）CSP 等安全標頭、`/ws` Origin 檢查、審計日誌、session 絕對逾時與雙鍵登入鎖定

---

## 二、資訊安全評估 — 風險發現清單

嚴重度定義：🔴 高（醫院上線前必須處理）、🟠 中（強烈建議上線前處理）、🟡 低（可排程處理）。

| 編號 | 嚴重度 | 位置 | 風險說明 | 對應工作 | 狀態 |
|---|---|---|---|---|---|
| F-01 | 🔴 | ingest / 全部 Pi | **所有 Pi 共用同一個固定 token**：任何一台 Pi 的 telemetry.json 外洩（或維修時被複製），拿到 token 的人就能冒充任何機台送假資料，且無法只撤銷一台——只能全部換 token、逐台重設 | W-104 | ✅ 已處理 |
| F-02 | 🟠 | ingest.py | **無連線數上限、無 hello 逾時、無閒置逾時**：連上 8765 後不送任何資料的連線會永久佔用資源；區網內任何裝置可開大量連線耗盡伺服器（max_devices 只限制「已註冊裝置」，不限制 TCP 連線本身） | W-101 | ✅ 已處理 |
| F-03 | 🟠 | ingest.py / hub.py | **訊息無格式驗證**：單行上限 512KB 且原樣廣播給所有觀看端。持有 token 的異常裝置可送超大 params/wave 灌爆觀看端與伺服器記憶體（每觀看端佇列 300 則 × 512KB ≈ 150MB） | W-102 | ✅ 已處理 |
| F-04 | 🟠 | routes.py | **缺 HTTP 安全標頭**：無 CSP、X-Frame-Options、X-Content-Type-Options、Referrer-Policy、HSTS。本專案前端零外部資源，CSP 可以直接鎖 `default-src 'self'`，成本低效果好 | W-105 | ✅ 已處理（CSP 仍需人工開瀏覽器抽查，見下方） |
| F-05 | 🟠 | routes.py | **/ws 未驗證 Origin**：目前靠 SameSite=Lax cookie 擋跨站 WebSocket 挾持，單層防禦；加 Origin 同源檢查是標準縱深作法 | W-106 | ✅ 已處理 |
| F-06 | 🟠 | 全域 | **無審計日誌、無檔案日誌**：登入成功/失敗、管理操作（移除裝置、下載 CSV）只印在主控台，視窗一關全部消失。醫院環境需要可回溯的操作紀錄 | W-109、W-201 | ✅ 審計日誌已處理（W-109）；一般伺服器 log 落地仍待 W-201（Phase 2） |
| F-07 | 🟡 | ingest.py:47 | token 比對用 `!=`（非常數時間）。區網內利用時間差猜 token 難度高，但修正只要一行 `hmac.compare_digest` | W-103 | ✅ 已處理 |
| F-08 | 🟡 | auth.py | 登入鎖定**只看來源 IP**：護理站多人共用同一台電腦/出口 IP 時，任何人連錯 5 次密碼會鎖住所有人 10 分鐘（可被惡意利用做拒絕服務）。建議改「IP+帳號」併用計數 | W-107 | ✅ 已處理 |
| F-09 | 🟡 | auth.py | 看板模式（idle=0）session **永不過期且無總量上限**：長年運行下 session 與失敗記錄字典會累積；遺失的看板電腦其 session 無法被撤銷（沒有「登出所有裝置」） | W-107 | ✅ 已處理（絕對逾時＋定期掃除＋總量上限） |
| F-10 | 🟡 | auth.py:29 | PBKDF2 迭代 20 萬次（OWASP 2023 對 SHA256 的建議為 60 萬次）。雜湊格式已含迭代數，提高後舊帳號仍可登入，零風險升級 | W-108 | ✅ 已處理（60 萬次） |
| F-11 | 🟡 | aiohttp 預設 | 回應帶 `Server: Python/x.x aiohttp/x.x` 標頭，洩漏元件版本（給攻擊者選 exploit 用） | W-105 | ✅ 已處理 |
| F-12 | 🟡 | requirements.txt | 相依未鎖版本（`aiohttp>=3.8`）：醫院機器安裝時可能裝到與開發環境不同的版本，行為不可重現，也無升級管控程序 | W-110 | ✅ 已處理（過程中另發現並修正 cp950 環境下 pip 解析 requirements.txt 失敗的問題） |
| F-13 | 🟡 | 部署面 | 憑證私鑰、accounts.json 無檔案權限規範（Windows 上預設同機使用者皆可讀）；**ca.key（發證私鑰）長駐伺服器**——被拿走等於能簽出任何受信任憑證 | W-204 | ⬜ 待處理（Phase 2，需在正式伺服器上執行） |
| F-14 | 🟡 | auth.py | 登入表單無 CSRF token（SameSite=Lax 已擋跨站帶 cookie 的 POST，殘餘風險為「login CSRF」，院內網情境影響很小） | W-107（低優先，併入處理） | ⬜ 評估後暫不處理（見 W-107 說明：院內網情境下複雜度與效益不成比例） |
| F-15 | 🟡 | hub.py | 觀看端（WebSocket viewer）數量無上限：登入者開大量分頁可耗伺服器記憶體 | W-102 | ✅ 已處理 |
| F-16 | 🟠 | 部署面 | **無服務化**：程式崩潰或伺服器重開機後不會自動恢復——中央監視這種「要一直活著」的系統，可用性本身就是安全需求 | W-202 | ⬜ 待處理（Phase 2） |
| F-17 | 🟡 | hub.py | 同 token 者可用相同 device id 頂替既有裝置（conn_seq 設計上允許重連）。做完 W-104（每台 token 不同）後此風險自然收斂；另建議 log 記錄來源 IP 變化以利追查 | W-104 | ⚠️ 部分處理：核心風險已消除（每台獨立 token）；裝置被拒事件的來源 IP 已進審計日誌，但裝置正常上線事件目前未記來源 IP，尚無自動化比對 |

> 補充說明（不列風險、屬確認事項）：波形/參數不落地，病人代碼只在記憶體與畫面。警報歷史已依後續需求落地，但只含機台編號、警報內容與起訖時間，SQLite 自動清除超過 7 天的已結束 episode。

---

## 三、使用功能評估

### 3.1 已完成且品質良好的功能

- 多床即時儀表板：三波形（抖動緩衝 + 平滑播放，永不暫停）、通氣模式、設定值、量測值、警報列、卡片放大
- 裝置雙重狀態呈現：「呼吸器連線」與「Pi 伺服器連線」分開顯示，離線時舊資料清空避免誤導
- 管理頁（admin 專屬）：全裝置健康總表（CPU/記憶體/溫度/磁碟/降頻/開機時長）、趨勢圖、移除離線裝置、sys CSV 下載、帳號唯讀清單
- 深/淺色主題、自動重連、session 過期自動導回登入頁
- 協議層容錯：未知 type 忽略（新舊版本混跑不炸）、snapshot+delta、心跳+watchdog
- 開發配套成熟：fake_pi 模擬器（含警報/過熱模擬）、單元測試、涵蓋面很廣的端對端冒煙測試

### 3.2 功能缺口

| 編號 | 缺口 | 影響 | 對應工作 | 狀態 |
|---|---|---|---|---|
| G-01 | 儀表板畫面上**沒有**「僅供觀察參考，不可作為臨床警報依據」的常駐聲明（只寫在 README） | 醫療法規/院方責任界定：使用者看不到免責聲明 | W-301 | ✅ 已處理 |
| G-02 | 無警報歷史（只顯示當下警報，發生/解除即消失） | 事後無法回答「昨晚 3 床何時響過什麼警報」 | W-302 | ✅ 已處理 |
| G-03 | 無波形/參數歷史儲存與回放（CLAUDE.md §10 路線圖項目） | 屬大型功能，建議醫院穩定上線後再評估 | W-305（選做） | ⬜ 刻意不做（選做項目） |
| G-04 | 帳號管理只有伺服器 CLI；使用者無法自行改密碼，admin 也無法在網頁上重設 | 換密碼要到機房操作，實務上會導致「永遠不換密碼」 | W-307（LDAP/AD，改密碼走醫院 HIS 既有流程） | ✅ W-307 已完成（local 為預設、ldap 已可用，待資訊室提供連線資訊即可切換）；W-303（自助改密／管理員重設）已評估後**移除**，理由見 W-303 狀態欄 |
| G-05 | 裝置離線只有卡片變灰，無聲音/醒目提醒 | 護理站沒盯著看時，Pi 斷線可能很久沒人發現 | W-304 | ✅ 已處理（2026-07-17 你決定**移除離線提示音**，離線只保留高對比視覺提醒；警報音改為「呼吸器警報」專用，靜音鈕在卡片警報列，見 W-304 註記） |
| G-06 | 無伺服器自身健康端點（/healthz）與版本號顯示 | 醫院資訊室無法納入監控；出問題時說不清楚跑的是哪一版 | W-203、W-306 | ✅ 已處理 |
| G-07 | 無 Windows 服務化、開機自啟、當機自動重啟 | 停電復電後要有人去雙擊 bat 檔 | W-202 | ✅ 腳本已完成並驗證語法；正式套用留待 Phase 5 |
| G-08 | 無備份/還原程序（config、accounts、certs） | 伺服器硬碟故障 = 全部重建 | W-205 | ✅ 已提供手動腳本；無外部備份空間時不排程，alarm/sys 七天歷史不備份 |
| G-09 | **git 目前有 14 個已修改 + 10 個未追蹤檔案未 commit**（整套登入/TLS/管理頁功能都不在版本控制內）、無版本標籤 | 無法回溯、無法確定部署的是哪一版 | W-001、W-002 | ✅ 已處理 |
| G-10 | 缺「給醫院資訊室的部署與安全說明書」與「護理站操作手冊」 | 進醫院一定會被資訊室要求提供 | W-405 | ⬜ 待處理（Phase 4） |

---

## 四、改善計畫（分階段工作清單）

工作量標記：**S** ≈ 半天內、**M** ≈ 1–2 天、**L** ≈ 3 天以上。
每個 Phase 結束時：跑全部自動化測試 → commit → 打 tag。

### Phase 0 — 基線整理（先做，半天）

| 編號 | 工作內容 | 產出 | 測試/驗收 | 量 |
|---|---|---|---|---|
| W-001 | 把未 commit 的成果分批入庫：①登入與 session（auth.py、login/auth 前端、make_user.py、accounts.json.example）②TLS（make_certs.py、main.py/ingest 修改）③管理頁（admin.*、sys.js、routes 修改）④文件（PROTOCOL.md、README） | git status 乾淨 | T-001：`python -m unittest discover -s tests` 與 `python tests/smoke_test.py` 全過後才 commit | S |
| W-002 | 建立版本標籤 `v0.9.0`（醫院前基線），新增 CHANGELOG.md；之後每個 Phase 完成打一個 tag | tag + CHANGELOG | T-002：`git tag` 可見；CHANGELOG 記載本計畫文件編號 | S |

### Phase 1 — 資安強化（伺服器程式；醫院上線前必須全部完成）

| 編號 | 工作內容 | 產出 | 測試/驗收 | 量 |
|---|---|---|---|---|
| W-101 | ingest 連線防護（F-02）：①同時 TCP 連線數上限（config `ingest_max_conns`，預設 64，超過直接拒絕）②連線後 10 秒內沒收到合法 hello → 斷線 ③已註冊連線閒置 60 秒無任何資料 → 斷線（Pi 端本來就每 2 秒 ping，正常不受影響） | ingest.py 修改 + config 新欄位 + PROTOCOL.md 補充 | T-101：新增整合測試——連上不送資料 10 秒被斷；開 65 條連線第 65 條被拒；正常 fake_pi 不受影響；smoke 全過 | M |
| W-102 | 訊息驗證與資源上限（F-03、F-15）：①MAX_LINE 由 512KB 降為 64KB ②wave 驗證 p/f/v 為等長數值陣列且 ≤2000 樣本，trig 為整數陣列 ③params 的 settings/measured 鍵數 ≤200、字串長度上限 ④違規記 log 後丟棄該訊息（不斷線，容忍 Pi 端 bug）⑤觀看端數量上限（config `max_viewers`，預設 50） | ingest.py/hub.py 驗證函式 | T-102：注入測試腳本（tools/bad_pi.py 或加入 smoke）——超長行、超大陣列、錯型別欄位都被丟棄且伺服器不崩潰、其他裝置不受影響 | M |
| W-103 | token 比對改 `hmac.compare_digest`（F-07） | ingest.py 一行修改 | T-103：smoke 既有「錯誤 token 被斷線」仍過 | S |
| W-104 | **每台 Pi 獨立 token**（F-01、F-17）：①新增 `devices.json`（device_id → token 雜湊、備註、enabled 旗標；不進 git，比照 accounts.json）②`tools/make_device.py`（產生/停用/列出，token 隨機產生一次性顯示）③hello 驗證改查 devices.json（保留舊模式：devices.json 不存在時退回單一 ingest_token，向後相容）④hello 來源 IP 與上次不同時記 log ⑤PROTOCOL.md、Pi 端部署文件同步更新 | devices.json.example、make_device.py、ingest/hub 修改 | T-104：smoke 新增——A 機 token 連 A 通過；A 機 token 冒充 B 機 device_id 被拒；停用（enabled=false）後被拒；舊單一 token 模式仍可用 | L |
| W-105 | HTTP 安全標頭 middleware（F-04、F-11）：CSP `default-src 'self'`、`X-Frame-Options: DENY`、`X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer`、TLS 時加 HSTS；移除/覆寫 Server 標頭 | web/ 新 middleware（掛在 create_app） | T-105：smoke 新增標頭斷言；人工開瀏覽器確認儀表板/管理頁/登入頁功能與主題切換全部正常（CSP 最容易誤傷，需實測） | M |
| W-106 | /ws 加 Origin 同源檢查（F-05）：Origin 存在且 host 不符 → 403 | routes.py | T-106：smoke 新增偽 Origin 的 ws 連線被拒；正常前端連線不受影響 | S |
| W-107 | session 與登入強化（F-08、F-09、F-14）：①session 總量上限 + 每小時清掃過期項 ②新增 config `session_absolute_hours`（預設 0=不限，看板模式用；一般帳號建議 12）③登入鎖定改「IP+帳號」雙鍵計數 ④登入表單加隨機 CSRF token（一次性，存 session 前置字典） | auth.py 修改 | T-107：新增 tests/test_auth.py 單元測試——雜湊驗證、閒置過期、絕對過期、清掃、鎖定（同帳號跨 IP、同 IP 跨帳號）、CSRF 缺失被拒 | M |
| W-108 | PBKDF2 迭代升至 600,000（F-10）；make_user.py 提示重建舊帳號 | auth.py 常數修改 | T-108：test_auth.py——舊 200k 雜湊帳號仍可登入；新帳號為 600k | S |
| W-109 | 審計日誌（F-06）：`logs/audit.log`（RotatingFileHandler，10MB×5）記錄——登入成功/失敗（帳號+IP）、登出、session 過期登出、管理操作（移除裝置、下載 CSV、帳號清單查看）、裝置上線/離線/被拒（含來源 IP）。**紅線：絕不寫入病人代碼與任何密碼/token 值** | web/ 或 domain/ 新模組 audit.py | T-109：smoke 後檢查 audit.log 含上述事件、grep 確認無病人代碼（SMOKE 開頭字串只能出現在裝置欄位外的地方＝不得出現）；輪替測試 | M |
| W-110 | 鎖定相依版本（F-12）：requirements.txt 改 `aiohttp==x.y.z`、`cryptography==x.y`；README 補「升級 SOP：改版→乾淨 venv 安裝→跑全部測試→soak 一晚→才上正式機」 | requirements.txt、README | T-110：全新 venv `pip install -r requirements.txt` + 全部測試通過 | S |

**Phase 1 完成定義**：F-01～F-15 全數關閉或降級為「已接受之殘餘風險」；smoke_test 擴充後全過；打 tag `v0.10.0`。

**✅ 已於 2026-07-07 完成並打 tag `v0.10.0`。** 單元測試由 17 項擴充到 50 項、冒煙測試新增連線防護/裝置權杖/安全標頭/Origin/審計日誌等斷言，全數通過。F-14（CSRF）評估後決定暫不處理（見 W-107 說明）；F-13/F-16 需要正式伺服器環境才能執行，留待 Phase 2/5。

**尚需人工確認的項目**（自動化測試涵蓋不到）：
- CSP 安全標頭上線後請實際開瀏覽器過一次儀表板/管理頁/登入頁＋深淺主題切換，確認沒有功能被誤傷（W-105）
- 長時間運行下 session 掃除、ingest 連線逾時等行為建議在 Phase 4 的 72 小時 soak 測試中一併觀察

### Phase 2 — 營運強化（Windows 伺服器）

| 編號 | 工作內容 | 產出 | 測試/驗收 | 量 |
|---|---|---|---|---|
| W-201 | 伺服器主 log 同步寫檔 `logs/server.log`（輪替同 W-109；主控台照印）；注意 cp950 問題——檔案一律 UTF-8 | main.py logging 設定 | T-201：重導向與雙寫皆正常、中文不亂碼、輪替生效 | S |
| W-202 | 服務化與自動復原（F-16、G-07）：用 Windows 工作排程器——「開機時啟動」+「失敗後 1 分鐘重啟」，以專用低權限帳號執行；寫成一鍵設定說明（或 setup 腳本）；start_server.bat 保留為手動除錯用 | 設定文件/腳本 | T-202：重開機後服務自動起來、儀表板可登入；工作管理員強殺 python 程序 → 1 分鐘內自動復活且 Pi 自動重連 | M |
| W-203 | 健康檢查端點（G-06）：`GET /healthz` 免登入，只回 `{"ok":true,"version":"x.y.z","devices":N,"uptime_s":N}`——不含裝置清單、不含病人資訊；版本號集中定義一處 | routes.py + version 常數 | T-203：未登入 curl 可取得；回應不含任何裝置/病人字串；smoke 加一條 | S |
| W-204 | 檔案權限與金鑰保護（F-13）：①icacls 指令鎖 `accounts.json`、`devices.json`、`config.json`、`certs/`、`logs/` 僅服務帳號與管理者可讀 ②**ca.key 離線保存**：簽完憑證後把 ca.key 移到離線 USB（兩份），伺服器上刪除；要重簽時再暫時取回 ③寫入部署文件 | 權限設定文件/腳本 | T-204：以一般使用者帳號嘗試讀取被拒；伺服器刪除 ca.key 後正常運作（server.pem/key 不受影響） | S |
| W-205 | 備份/還原（G-08）：有外部空間時，以腳本備份 config.json、accounts.json、devices.json、certs/server.*、logs/audit.log；alarm/sys 七天歷史 DB 不備份 | tools/backup.ps1 + 文件 | T-205：有備份目的地時做一次「刪掉 accounts.json → 從備份還原 → 可登入」演練 | M |
| W-206 | 時間同步：確認伺服器與每台 Pi 都同步院內 NTP（或伺服器兼作 NTP 源）；寫入部署文件（警報/審計時間戳可信度的前提） | 文件 | T-206：`w32tm /query /status`（Win）與 Pi `timedatectl` 皆同步 | S |
| W-207 | 長期資源監控：沿用/擴充 tools/soak_monitor.ps1，紀錄伺服器程序記憶體/CPU 趨勢，異常門檻寫進文件（例：RSS 連續 3 天上升 >10%/天 需回報） | 腳本 + 判讀標準 | T-207：跑 24 小時產出 CSV 可判讀 | S |

**Phase 2 完成定義**：伺服器可無人值守運行（自動啟動、自動復活、有日誌、有備份）；打 tag `v0.11.0`。

**✅ 已於 2026-07-07 完成並打 tag `v0.11.0`。** W-201/203/205/206/207 為程式/腳本/文件，
已完成並在開發環境驗證（腳本用 `-WhatIf`/`-DryRun` 確認語法與行為，未在開發機上
留下真實系統變更）；W-202/204 的**實際套用**（真的在伺服器上建立排程工作、真的鎖
檔案權限）依性質必須在正式伺服器上執行，留待 Phase 5 醫院移轉時進行。

### Phase 3 — 功能補強

| 編號 | 工作內容 | 產出 | 測試/驗收 | 量 |
|---|---|---|---|---|
| W-301 | 免責聲明常駐（G-01）：儀表板與管理頁 footer 固定顯示「本畫面僅供觀察參考，不可作為臨床警報依據」；登入頁同句聲明；深淺主題都清晰可讀 | index/admin/login.html + style.css | T-301：兩主題視覺檢查；文案經你（案主）確認 | S |
| W-302 | 警報歷史（G-02）：`domain/alarm_log.py`（Hub 訂閱者）——以 `logs/alarm_logs/alarm_history.sqlite3` 記錄每台裝置的警報 episode（開始/結束/持續時間/狀態/代碼/文字/優先級），已結束資料保留 7 天；作用中不逾期刪除，離線/重啟標記結束不明。**只記機台編號，不記病人代碼、波形或量測值**；管理頁依機台匯出 CSV，詳細畫面可查最近紀錄；警報 DB 不納入備份 | 新模組 + admin/詳細畫面前端 + PROTOCOL.md | T-302：episode 建立/解除/離線/重啟/7 天清除；依機台查詢；下載限 admin | M |
| W-303 | ~~密碼自助管理（G-04）~~ **已於 2026-07-07 移除**：`POST /api/password`（自助改密）與 `POST /api/admin/reset-password/{username}`（管理員重設）連同前端「改密碼」按鈕/選單項目全部拿掉。理由：確定要做 W-307（LDAP/AD）取代本機密碼管理，兩套並存會讓「密碼到底該去哪裡改」變得混亂——local 模式下密碼固定由管理員執行 `tools/make_user.py` 重設，ldap 模式下密碼完全交給院內 HIS/AD | auth.py/routes/前端（移除） | 迴歸：smoke_test.py 移除對應測試段，其餘測試全數通過 | S |
| W-304 | 裝置離線醒目提醒（G-05）：離線卡片改高對比警示樣式 + ~~可選提示音~~。**2026-07-17 變更**：你決定離線**不出聲**（提示音整組移除，含 header 開關），聲音改為**呼吸器警報音**——呼吸器發出 level 1/2 警報時鳴響（level 1 三連短高音/2.5s、level 2 兩連低音/5s，取全場最嚴重者），靜音鈕放在該卡片警報列（只靜音該台；警報全數解除自動復歸；Pi 離線不鳴響）。**對外通知（LINE/Email）觸犯資料不出院內網紅線，不在本計畫內**，日後有需求另案討論 | app.js/style.css/index.html | T-304：fake_pi Ctrl+C 後 5 秒內卡片變警示樣式（無聲）；`fake_pi.py --alarms` 有聲、卡片靜音鈕只靜音該台 | S |
| W-305 | （選做，醫院穩定後再議）歷史波形/參數儲存與回放：`domain/storage.py`（SQLite）+ 查詢 API + 回放前端。涉及臨床資料落地，動工前需先確認院方資料保存政策與加密要求 | — | — | L |
| W-306 | 版本資訊（G-06）：header 或登入頁顯示版本號（同 /healthz 的 version） | 前端小改 | T-306：畫面版本 = git tag | S |
| W-307 | ✅ **已完成**（2026-07-07 你已核准新增 `ldap3` 相依）**院內帳號整合（LDAP/AD 單一密碼來源）**：比照 MAYA(RCS) 的作法——登入時把帳密交給醫院集中帳號系統（LDAP / Active Directory）驗證，本系統不再自己存密碼；密碼從醫院 HIS/AD 端改，這裡立刻生效。設計見本文件下方「六之一、LDAP/AD 帳號整合說明」。`monitor/web/ldap_auth.py`（`LdapAuthenticator`）＋ `monitor/web/auth.py` 改依賴注入（`AuthManager` 不再寫死 `LocalAuthenticator`）＋ `config.json` 新增 `auth_backend`（`local`/`ldap`）等欄位；**預設仍是 `local`**，`ldap` 模式待資訊室提供實際伺服器/DN 格式後只需改設定檔即可切換，不用改程式碼。**2026-07-17 補強**：新增 `ldap_ca` 設定——ldaps 連線改以院內 CA 根憑證驗證 AD 伺服器憑證（`CERT_REQUIRED`；未設定維持只加密不驗證並於啟動時警告，正式環境必填） | `monitor/web/ldap_auth.py`、`monitor/web/auth.py`、`main.py`、`monitor/config.py` | T-307：`tests/test_ldap_auth.py`（8 項，`ldap3` MOCK_SYNC 模擬 bind 成功/失敗/未知帳號/不在白名單/空密碼等情況）全數通過；`ldap3==2.9.1` 已鎖定版本，僅 `auth_backend=ldap` 時需要安裝 | M |

**Phase 3 完成定義**：G-01、G-02、G-04、G-05 關閉；打 tag `v1.0.0-rc1`。

**✅ 已於 2026-07-07 完成並打 tag `v1.0.0-rc1`。** W-301/302/304/306 全數完成
（單元測試累計達 60 項，smoke_test.py 同步擴充涵蓋警報歷史等新端點）。
W-305（歷史波形回放）依計畫維持選做、不做。

**2026-07-07 後續變更**：你確認要用 LDAP/AD 取代本機密碼自助管理，於是 W-307 提前
實作（不再是選做），同時 W-303（密碼自助管理）評估後**整組移除**（見上方 W-303
狀態欄的理由）；順便把 header 一整排按鈕（深淺色/離線提示音/設備管理/登出）收整
成點帳號名稱展開的下拉選單，減少視覺雜訊。單元測試增至 68 項（新增
`tests/test_ldap_auth.py`），smoke_test.py 移除已不存在端點的測試段，其餘全數通過。

### Phase 4 — 部署前整體驗證（在自家/辦公室環境完成）

| 編號 | 工作內容 | 驗收標準 | 量 |
|---|---|---|---|
| W-401 | **72 小時 soak**：5 台 fake_pi（含 1 台 --alarms、1 台 --sys-hot）+ 2 個瀏覽器觀看端連續運行，W-207 腳本監控 | T-401：記憶體無趨勢性成長（<5%/72h）、零未預期錯誤 log、波形全程流暢 | M |
| W-402 | 資安自我檢測：①`nmap` 掃伺服器——只有 8765/8080 開放 ②未登入存取全部端點被拒（自動化已涵蓋，人工再抽查）③瀏覽器開發者工具檢查安全標頭與 cookie 旗標 ④TLS 檢查（協議版本 ≥1.2、憑證鏈正確）⑤帳號清查——無測試帳號、無弱密碼 ⑥grep 全部 log 確認無病人代碼、無 token 明碼 | T-402：檢測表逐項簽署，全過 | M |
| W-403 | 故障演練：①拔伺服器網線 30 秒→接回 ②強殺伺服器程序 ③伺服器重開機 ④Pi 斷電重開 ⑤某台 Pi 時鐘偏移 10 分鐘 | T-403：每項恢復後 2 分鐘內全系統自動回復正常，無需手動介入；審計日誌完整記錄事件 | M |
| W-404 | 效能驗證：16 台 fake_pi + 10 個觀看端 | T-404：伺服器 CPU 個位數 %、端到端延遲 <2 秒、瀏覽器不掉幀（CLAUDE.md §2.5 預算） | S |
| W-405 | 文件交付（G-10）：①「醫院資訊室部署與安全說明書」——架構圖、埠與資料流、資料分類（病人代碼僅記憶體/畫面）、無網際網路相依、備份策略、防火牆需求單 ②「護理站操作手冊」——登入、看板、警報顯示含義、免責聲明 ③「管理員手冊」——帳號/裝置 token 管理、備份還原、緊急重啟 SOP | T-405：三份文件由非開發者（你自己以使用者身分）照著操作一遍可完成 | M |

### Phase 5 — 醫院移轉 SOP（照順序執行）

| 編號 | 工作內容 | 驗收 |
|---|---|---|
| W-501 | 環境準備：向資訊室提交需求清單（**見附錄 C，可直接列印/複製**），確認：①伺服器主機＋固定 IP（有線院內網段，比照現有系統所在網段）②防火牆放行 8765/8080，僅限院內網段來源，不對外網開放 ③Python 3.9+ 安裝許可、服務帳號 ④（建議）內部 DNS 名稱指向固定 IP，比照 `maya-ap.csh.org.tw` 的做法 ⑤（建議）是否有內部憑證簽發機構（Internal CA）可直接簽發正式憑證 ⑥呼吸器所在病房網段與伺服器網段的路由是否相通 ⑦院內 NTP 位址 | T-501：申請單核准；`python --version` 正確；從護理站網段可連到伺服器 8080/8765，從外部網路連不到（資訊室協助驗證或自行用手機關閉 Wi-Fi 改行動網路測試連不到） |
| W-502 | 憑證重簽：**若資訊室能提供內部 CA 簽發**→直接採用他們核發的憑證檔（`tls_cert`/`tls_key` 指過去），瀏覽器不會跳警告，省去逐台匯入 CA 的步驟；**若無**→伺服器上 `python tools/make_certs.py --host <醫院固定IP或內部網域名稱>`（沿用既有 CA → Pi 端 ca.pem 不用換），把 ca.pem 部署到每台 Pi 與看板電腦、ca.key 離線保存（見 W-204） | T-502：瀏覽器 https 無憑證錯誤（自簽 CA 情況下需先匯入）；Pi TLS 連線成功 |
| W-503 | 憑據全面換新：**開發期所有 token/密碼一律不沿用**——每台 Pi 用 make_device.py 發新 token；建立正式帳號（admin×1、看板 viewer×N）；刪除所有測試帳號；accounts/devices/config 檔案權限套 W-204 | T-503：make_user.py --list 與 make_device.py --list 只有正式項目；舊 token 連線被拒 |
| W-504 | 漸進接入驗證：①院內另一台電腦跑 fake_pi 打通全鏈 ②接**一台**真 Pi 驗證 24 小時 ③再全數接入 | T-504：每階段儀表板顯示正常、audit.log 無異常、資訊室防火牆無阻擋記錄 |
| W-505 | 觀察期 1–2 週：W-207 監控腳本運行；每日檢視 audit.log 與資源趨勢；建立問題回報管道（誰發現異常通知誰） | T-505：觀察期滿無 P1/P2 事件 |
| W-506 | 驗收交付：備份排程已生效且演練過還原、三份文件交付院方、緊急重啟 SOP 紙本貼在伺服器旁、打 tag `v1.0.0` 並記錄部署 commit hash | T-506：交付清單逐項簽收 |

---

## 五、測試項目總表（執行時逐項勾選）

| 測試編號 | 內容 | 型態 | 對應工作 |
|---|---|---|---|
| T-001 | 全部既有測試通過後入庫 | 自動 | W-001 |
| T-002 | 版本標籤與 CHANGELOG 存在 | 人工 | W-002 |
| T-101 | 無 hello 連線 10s 被斷；連線數超限被拒；正常裝置不受影響 | 自動 | W-101 |
| T-102 | 畸形/超大訊息被丟棄且不崩潰、不影響他床 | 自動 | W-102 |
| T-103 | 錯誤 token 仍被斷線（回歸） | 自動 | W-103 |
| T-104 | 每台獨立 token：正確通過、冒充被拒、停用被拒、舊模式相容 | 自動 | W-104 |
| T-105 | 安全標頭齊全；CSP 下三頁面功能正常 | 自動+人工 | W-105 |
| T-106 | 偽 Origin 的 /ws 被拒 | 自動 | W-106 |
| T-107 | session 過期/清掃/上限、雙鍵鎖定、CSRF | 自動 | W-107 |
| T-108 | 新舊迭代數帳號皆可登入 | 自動 | W-108 |
| T-109 | 審計日誌事件齊全、無病人代碼、輪替正常 | 自動+人工 | W-109 |
| T-110 | 乾淨環境依鎖定版本安裝並通過全部測試 | 人工 | W-110 |
| T-201 | 檔案日誌雙寫、UTF-8、輪替 | 人工 | W-201 |
| T-202 | 重開機自啟；強殺 1 分鐘內復活 | 人工 | W-202 |
| T-203 | /healthz 可用且無敏感資訊 | 自動 | W-203 |
| T-204 | 檔案權限鎖定；ca.key 已離線 | 人工 | W-204 |
| T-205 | 備份→刪除→還原演練成功 | 人工 | W-205 |
| T-206 | 伺服器與 Pi 時間同步 | 人工 | W-206 |
| T-207 | 24h 資源監控 CSV 可判讀 | 人工 | W-207 |
| T-301 | 免責聲明兩主題可讀、文案核可 | 人工 | W-301 |
| T-302 | SQLite episode 建立/解除/離線/重啟/7 天清除；依機台查詢；下載限 admin | 自動 | W-302 |
| T-303 | ~~改密碼流程；權限隔離~~（功能已移除，見 W-303 狀態欄） | — | W-303 |
| T-304 | 離線 5 秒內醒目提示；提示音開關有效 | 人工 | W-304 |
| T-306 | 畫面版本號 = tag | 人工 | W-306 |
| T-401 | 72h soak 達標 | 人工 | W-401 |
| T-402 | 資安檢測表全過 | 人工 | W-402 |
| T-403 | 五項故障演練全部自動恢復 | 人工 | W-403 |
| T-404 | 16 床×10 觀看端效能達標 | 人工 | W-404 |
| T-405 | 三份文件可照做 | 人工 | W-405 |
| T-307 | LDAP mock server 驗證 bind 成功/失敗/未知帳號/不在白名單/空密碼對應正確登入結果 | 自動（`tests/test_ldap_auth.py`，8 項全過） | W-307 |
| T-501–T-506 | 醫院移轉各階段驗收 | 人工 | W-501–W-506 |

---

## 六、醫院上線最終檢核表（Go / No-Go）

上線當天逐項確認，**任一項未勾即不開放使用**：

- [ ] TLS 啟用（config 指向醫院 IP 憑證），瀏覽器與 Pi 皆驗證通過
- [ ] auth_enabled=true；只有正式帳號，無測試/預設帳號，密碼皆 ≥8 碼
- [ ] 每台 Pi 獨立 token（devices.json），開發期 token 已全部作廢
- [ ] ca.key 已離線保存並自伺服器移除；敏感檔案 ACL 已鎖
- [ ] 防火牆僅放行 8765/8080 且來源限監測網段
- [ ] 服務自動啟動與自動重啟已驗證（實際重開機測過）
- [ ] audit.log / server.log 正常輪替寫入；日誌中無病人代碼
- [ ] 若院方提供外部備份空間，備份排程生效且做過還原演練
- [ ] NTP 時間同步（伺服器 + 全部 Pi）
- [ ] 儀表板常駐免責聲明顯示中
- [ ] /healthz 正常；畫面版本號 = 部署 tag
- [ ] 護理站/管理員/資訊室三份文件已交付
- [ ] 緊急重啟 SOP 紙本已貼於伺服器旁

---

## 七、LDAP/AD 帳號整合設計說明（對應 W-307，已於 2026-07-07 實作完成）

### 7.1 原理：MAYA(RCS) 為什麼改 HIS 密碼就能生效？

這類系統通常**不會自己存密碼**，而是每次登入時把你輸入的帳號密碼轉交給醫院的**集中帳號目錄服務**（最常見是 **LDAP** 或 **Active Directory，AD**——AD 是微軟實作的 LDAP，醫院多半用這套，因為 Windows 網域登入、信箱、內部系統常共用同一套）做驗證，流程是：

1. 你在 MAYA 登入頁輸入帳號密碼、按登入
2. MAYA 後台拿這組帳密去問醫院的 AD 伺服器：「這組帳密對不對？」（技術上叫 **LDAP bind**——用這組帳密去嘗試連線 AD，連得上就是對的，連不上就是錯的）
3. AD 回答「對」或「錯」，MAYA 依此決定放不放你進去

因為密碼的「正確答案」永遠只存在 AD 裡一份，MAYA 從來沒有自己存過你的密碼——**你在 HIS/AD 端改密碼的當下，全院所有用同一套驗證方式的系統都立刻生效**，因為它們每次登入都是「現場問 AD」，不是「跟自己存的舊密碼比對」。

這跟真正的「單一登入 SSO」（登入一次、其他系統自動免密碼放行，例如 Windows 網域帳號登入電腦後開啟內部網頁不用再輸入帳密）不完全一樣——SSO 還需要 Kerberos/SAML/OAuth 等機制讓瀏覽器自動帶身分憑證過去。MAYA 的登入頁仍要你手動輸入帳密，所以**大機率是 LDAP bind 這種「每次現場驗證」的作法**，不是完整 SSO。這對我們來說是好消息：LDAP bind 的做法比完整 SSO 單純很多，改動範圍小。

### 7.2 實作方式（已完成）

`monitor/web/auth.py` 的 `AuthManager` 改為依賴注入：建構子接受一個 `authenticator` 物件（不再寫死 `LocalAuthenticator`），只要求它實作 `authenticate(username, password) -> role 字串或 None`、`has_users()`、`list_users()` 三個方法。登入頁、session 管理、middleware、管理頁權限判斷**完全不用動**。`main.py` 新增 `build_authenticator(cfg)`，依 `config.json` 的 `auth_backend` 欄位（`"local"` 或 `"ldap"`）決定要建立哪一種驗證器——這是 Strategy pattern，切換後端不影響其他程式。

### 7.3 設計方案（已實作，見 `monitor/web/ldap_auth.py`）

- 新增 `LdapAuthenticator`（`monitor/web/ldap_auth.py`）：
  - 用 Python 套件 **`ldap3`**（已核准新增、鎖定版本 `ldap3==2.9.1`，僅 `auth_backend=ldap` 時才需要安裝）對醫院的 AD 伺服器做 bind——**強烈建議搭配 LDAPS**（LDAP over TLS，636 埠；`ldap_use_ssl` 預設 `true`）避免密碼明文上網路，實際部署時仍需依資訊室提供的埠號確認
  - bind 成功 = 密碼正確；失敗（含連線異常）= 一律視為驗證失敗，不區分「密碼錯誤」或「帳號不存在」（避免洩漏帳號是否存在）
  - 每次登入才建立連線、bind、關閉，不維持常駐連線池（登入頻率低，簡單可靠優先於效能）
- **角色（viewer/admin）不交給 AD 決定**：AD 只負責回答「密碼對不對」，「這個人能不能用本系統、給 viewer 還是 admin」仍由本機一份小名單決定（沿用 `accounts.json` 的 `{"users":[{"username":...,"role":...}]}` 格式，`password` 欄位此模式下不會被讀取）。理由：AD 群組結構是醫院整體 IT 治理範圍，我們不該也無法自行決定「哪個 AD 群組=可以用呼吸器監測系統」
- **LDAP 連不上時怎麼辦**：fail closed，一律拒絕登入，不會退回本機密碼比對（那樣會讓「已經在 HIS 停用的帳號」在本系統還能用舊密碼登入）。目前**沒有**做本機備援帳號機制——上線前若需要這項風險权衡，要再另外討論

### 7.4 目前狀態：程式已完成，卡在等資訊室資料

程式碼、單元測試（`tests/test_ldap_auth.py`，用 `ldap3` 內建 `MOCK_SYNC` 策略模擬 LDAP 目錄，共 8 項）皆已完成並通過；`config.json` 預設 `auth_backend` 仍是 `"local"`（帳號仍存 `accounts.json`，走 `tools/make_user.py` 管理，本系統已不提供改密碼功能，見 W-303）。要在醫院正式切到 `"ldap"` 模式，還需要向資訊室索取以下資訊（已併入附錄 C）並填入 `config.json` 對應欄位（`ldap_server`／`ldap_bind_template`／`ldap_use_ssl`／`ldap_timeout`）：

- AD/LDAP 伺服器位址與埠（要求 LDAPS 636，不接受明文 389）
- 帳號綁定格式（例如 `員工編號@csh.org.tw`，或需要先用一組服務帳號查一次使用者位置再驗證——目前程式只支援直接 bind，search-then-bind 需另外擴充，見 `ldap_auth.py` 開頭「已知限制」）
- 是否允許第三方內部系統直接對 AD 發送查詢／驗證（部分醫院基於資安政策不開放，會改提供一個驗證用的 Web API，那樣設計方式會不同，需要拿到 API 規格再重新討論）
- 這個議題該找資訊室哪個窗口（通常是網域管理/身分驗證系統負責人，不一定是幫 RCS 系統維運的人）

---

## 附錄 A — 風險 ↔ 工作對照

| 風險 | 工作 | | 風險 | 工作 |
|---|---|---|---|---|
| F-01 | W-104 | | F-10 | W-108 |
| F-02 | W-101 | | F-11 | W-105 |
| F-03 | W-102 | | F-12 | W-110 |
| F-04 | W-105 | | F-13 | W-204 |
| F-05 | W-106 | | F-14 | W-107 |
| F-06 | W-109/W-201 | | F-15 | W-102 |
| F-07 | W-103 | | F-16 | W-202 |
| F-08 | W-107 | | F-17 | W-104 |
| F-09 | W-107 | | | |

## 附錄 B — 建議執行順序摘要

```
Phase 0（半天）→ Phase 1（約 2 週）→ Phase 2（約 1 週）→ Phase 3（約 1 週）
→ Phase 4（1 週 + 72h soak）→ Phase 5（醫院端，1–3 週含觀察期）
```

原則：**每完成一個 W 項目就跑一次全部自動化測試並 commit**（一個 commit 一件事）；
每個 Phase 結束打 tag；任何協議變更先改 PROTOCOL.md（contract-first）再動程式。

## 附錄 C — 資訊室需求清單（可直接列印/複製使用，對應 W-501/W-502/W-307）

### C.1 伺服器與網路（第一次接洽一定要問）

> **需求：架設一台呼吸器監測儀表板伺服器，僅供院內網路存取**
>
> 1. **一台伺服器主機**（實體機或虛擬機皆可，Windows 或 Linux 皆可），安裝 Python 3.9 以上
> 2. **固定 IP**：請接在有線院內網段（比照現有系統所在網段），並指派一個**固定位址**（不要 DHCP 動態配發）
> 3. **防火牆規則**：只允許「院內網段」（含有線網段與員工 Wi-Fi 網段）連到這台伺服器的兩個 port：
>    - TCP 8765（l呼吸器監測裝置回傳資料用）
>    - TCP 8080（護理站瀏覽器看畫面用，未來可視需要改 443）
>    **明確要求：不對外網（Internet）開放這兩個 port**
> 4. **（建議）內部 DNS 名稱**：能否也給一個內部名稱（例如 `rt-monitor.csh.org.tw`）指向這台伺服器的固定 IP？護理站以後打名稱就好，不用記 IP
> 5. **（建議）是否有內部憑證簽發機構（Internal CA）**：若有，能否比照幫本系統的伺服器簽一張憑證？瀏覽器開啟時就不會跳「不安全」警告，也不用逐台匯入憑證
> 6. 請確認呼吸器所在病房的網路（有線或 Wi-Fi）跟這台伺服器的網段之間**有沒有路由相通**
> 7. 院內 **NTP 時間伺服器**位址
> 8. 若有資產登記/新設備報備流程，麻煩告知需要準備的資料
> 9. **遠端存取方式**：架設好之後，我可以用遠端桌面（RDP）從自己座位連進去操作嗎？還是第一次架設需要到資訊室/機房現場？
> 10. **對外網路存取**：這台伺服器能不能連到外部網際網路（例如 `github.com`、`pypi.org`）？我需要用它下載程式碼與安裝 Python 套件。**如果院內政策不開放對外連線（多數院內網段預期如此），麻煩告知貴院慣用的檔案傳入方式**（隨身碟掃毒後帶入、內部檔案伺服器等），我會事先把程式碼與所有需要的套件打包好再帶過去
> 11. **部署方式相容性**：我們的系統是一支獨立的 Python 服務，會自己監聽固定的 port（不透過 IIS），這樣的部署方式跟貴院的內部規範相容嗎？

### C.2 帳號密碼整合（選問——只有想做「密碼跟醫院系統同步」才需要問，對應 W-307）

> **需求：本系統的登入密碼是否能比照 MAYA(RCS) 串接貴院集中帳號系統驗證？**
>
> 1. 我們是否可以對貴院的 LDAP / Active Directory 發送驗證請求？
> 2. 若可以，請提供：
>    - 伺服器位址與埠（**需要 LDAPS 加密埠，通常是 636**，不使用明文 389）
>    - 帳號綁定格式（例如 `員工編號@csh.org.tw`，或是否需要先用一組服務帳號查詢使用者位置後才能驗證）
>    - 是否允許第三方內部系統直接發送 LDAP 查詢，或貴院有另外提供驗證用的 API（部分醫院基於資安政策不開放直接查詢）
> 3. 這個議題的窗口是誰（通常是網域管理/身分驗證系統負責人，可能跟 RCS 系統的維運窗口不同）
